import { GravedadPipePipe } from './gravedad-pipe.pipe';

describe('GravedadPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GravedadPipePipe();
    expect(pipe).toBeTruthy();
  });
});
