import { HabitacionPipe } from './habitacion.pipe';

describe('HabitacionPipe', () => {
  it('create an instance', () => {
    const pipe = new HabitacionPipe();
    expect(pipe).toBeTruthy();
  });
});
